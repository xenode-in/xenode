"use client";

// Re-export the shared design-system theme provider so Accounts, Drive, and
// Photos all theme identically from one source of truth (@xenode/ui).
export { ThemeProvider } from "@xenode/ui";
