export default function LoginPage() {
  return <main style={{ maxWidth: 480, margin: "0 auto", padding: 64 }}><h1>Sign in to Xenode</h1><p>Email or username uses the same generic credential response.</p></main>;
}
