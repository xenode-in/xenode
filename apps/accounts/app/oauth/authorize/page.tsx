export default function ConsentPage() {
  return <main style={{ maxWidth: 560, margin: "0 auto", padding: 64 }}><h1>Authorize product</h1><p>Only identity claims are issued here. Account Root Keys are never placed in OIDC tokens.</p></main>;
}
