# Generated ONLYOFFICE editor artifacts

`npm run onlyoffice:build-client` writes immutable, versioned browser assets to
this directory. Generated files are excluded from Git; production should deploy
them as separately cached static artifacts.

No standard ONLYOFFICE Document Server process is served from this path.
